find minimum length subarray with sum 0
write algoritm to find anagrams of a given key in the  file
convert btree to sorted linked list / doubly linked list.

Design a promotional rollout system.
users are bucketed based on attributes. a user can be in multiple buckets.
There are set of rules which decide which bucket to choose from.
High level design/ low level design.